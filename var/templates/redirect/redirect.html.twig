<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="refresh" content="2;url={{ url }}" />
    <title>Redirecting to {{ url }}</title>
  </head>
  <body>
    Redirecting to <a id="redirect-to" href="{{ url }}">{{ url }}</a>.
  </body>
</html>
